export type Location = {
    name: string,
    latitude: number,
    longitude: number
}


export type Place = {
    name: string,
    id: string
}

export type TravelType = "car" | "plane" | "train" | "bus";
